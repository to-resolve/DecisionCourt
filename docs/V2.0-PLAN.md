# v2.0 规划文档 — 厕所标识剪影小人庭审

> **版本升级说明（2026-08-21）**：本规划原属 v1.0.5，**升级为 v2.0**。用户重新评估后认为该方向工作量+调试周期都大，升格为大版本（破坏性变更预期）。
> **版本**: v2.0 规划（升级自 v1.0.5-PLAN.md，原 2026-08-20 规划方案保留作技术调研基础）
> **状态**: ⏸ **规划阶段，待 v1.0.4 完成后启动**
> **优先级**: P1（v1.0.4 后，视觉升级的最后一档 → 现在是 v2.0 主线）
> **目标 release**: v2.0 在 v1.0.4 基础上 2-3 周 / 3 PR（比 v1.0.5 多 1 个 PR，含视觉打磨）
> **触及 §2.1 裁决逻辑**: **0**（纯视觉替换，不影响庭审逻辑）
> **历史决策记录**：v1.0.5-PLAN.md (2026-08-20) → 2026-08-21 升格为 v2.0-PLAN.md

---

## 版本升级说明（2026-08-21）

### 升级原因

用户在 2026-08-21 重新规划讨论中明确表态："v1.0.5 原方案工作量+调试周期都大，升格为大版本 v2.0"。原剪影小人 SVG 方案升级（不是作废），但分配大版本号：
1. **工作量预期**：3 PR（v1.0.5 是 2 PR），多 1 个 PR 用于视觉打磨 + 用户反馈迭代
2. **调试周期预期**：动画效果需要用户多轮反馈，破坏性变更需做向后兼容（旧圆形头像保留作为 fallback）
3. **优先级升格**：从 v1.0.5 的"动画升级最后"升为 v2.0 主线

### 与原 v1.0.5-PLAN.md 的差异

| 维度 | v1.0.5 原方案 | v2.0 升级 |
|---|---|---|
| 工作量 | 1-1.5 周 | 2-3 周（多 1 PR） |
| PR 数 | 2 (D1 + D2) | 3 (D1 + D2 + D3 视觉打磨 PR) |
| 优先级 | P1 "动画升级最后" | v2.0 主线 |
| 调研深度 | 单人 1-1.5 周 | 多人多轮反馈，含破坏性兼容 |
| 测试覆盖 | 7 sub-test | 12+ sub-test（含视觉回归）|

---

## 以下为原 v1.0.5 规划内容（升级为 v2.0，方案不变，扩展工作量/PR 拆分）
3. 后续 v1.0.6 重新规划的参考（视觉升级仍可走剪影路线，但优先级降为 P3）

### 重新规划要求

新方向（v1.0.5 或 v1.0.6，由用户决定）应满足：
- 触及 §2.1 裁决逻辑：0（纯 UI/产品改进，不影响庭审流程）
- 不依赖已废弃的 ArgumentMap（已删除）作为前置
- 不引入"动画升级"硬性优先级（用户原话："动画升级最后"）
- 与 v1.0.4 顺序：v1.0.4 (Trace + Framer Motion) → v1.0.5+

### 备选方向（用户可选）

| 方向 | 工作量 | 触及 §2.1 |
|---|---|---|
| UX 体验重新设计（信息架构 + 交互模式） | 2-3 周 | 0 |
| Agent 行为改进（更智能的论据生成 / 反驳策略） | 2-3 周 | 中（§2.1 需先讨论）|
| 调研员体系加强（多源搜索 / 证据聚合） | 1-2 周 | 0 |
| 证据体系重构（多模态证据 / 时间戳 / 链上证据） | 3-4 周 | 0 |
| 国际化（i18n）| 1 周 | 0 |
| 其他（用户自定）| 视方向 | 视方向 |

---

# 以下为原规划内容（2026-08-20 规划，2026-08-21 作废）

> **版本**: v1.0.5 规划（用户 2026-08-20 授权"做之前先产出规划文档等文档"）
> **状态**: ❌ **规划已被用户重新规划，作废**
> **优先级**: ~~P1（v1.0.4 后，动画升级的最后一档）~~
> **目标 release**: ~~v1.0.5 在 v1.0.4 基础上 1-2 周 / 2-3 PR~~
> **触及 §2.1 裁决逻辑**: **0**（纯视觉替换，不影响庭审逻辑）

---

## 0. 上下文

### 0.1 用户原始需求（2026-08-20）
> "小人初步就是像是厕所标识上的那种小人就行，不要各种细节，颜色待定，动作就是走路双脚、手、点头啥的。然后这个先不进行，先和之前的待做合并一下"

---

## 0. 上下文

### 0.1 用户原始需求（2026-08-20）
> "小人初步就是像是厕所标识上的那种小人就行，不要各种细节，颜色待定，动作就是走路双脚、手、点头啥的。然后这个先不进行，先和之前的待做合并一下"

### 0.2 需求解读
- **风格**：厕所标识剪影风格（简洁、像素、对称剪影）
- **细节度**：不需要精致，剪影即可
- **颜色**：占位色（Tailwind 5 色 + CSS var），后期调
- **动作**：走路（双脚交替）、手部动作、点头
- **优先级**：**最后做**（用户原话"动画升级最后"）
- **前置依赖**：v1.0.4 Framer Motion 微动效基础设施

### 0.3 渐进升级策略
1. v1.0.4: 现有圆形头像 + 微动效（点头 / 敲锤 / 气泡）
2. v1.0.5: **替换为剪影小人 SVG**（带走路 / 点头 / 举手）
3. v1.0.x+: 后续可升级为 Lottie 立绘（设计师产出时再做）

**v1.0.5 不引入 Lottie/Rive**，因为：
- 用户明确"不需要细节"
- 设计师产出成本高
- 剪影 SVG + CSS 动画已足够满足需求

---

## 1. 设计方案

### 1.1 文件结构

```
frontend/
├── components/
│   └── courtroom/
│       └── silhouettes/           # NEW
│           ├── Silhouette.tsx     # 5 角色 SVG 组件
│           ├── SilhouetteAnimations.tsx  # 走路/点头/举手 CSS keyframes
│           └── RoleSilhouette.tsx # 根据 agent_type 选择 5 个 SVG 之一
├── styles/
│   └── silhouettes.css            # NEW — 走路/点头 CSS keyframes
```

### 1.2 SVG 设计原则（厕所标识风格）

**5 角色 SVG 设计**：
- 风格：纯色剪影（无细节、无渐变）
- 尺寸：64x64 或 96x96 viewBox
- 对称：左右对称剪影（无头发、衣服细节）
- 角色特征：
  - **控方律师**：圆形头 + 站立姿势 + 举手指控（指尖向上）
  - **辩方律师**：圆形头 + 站立姿势 + 双手打开辩护
  - **调查员**：圆形头 + 蹲下 + 拿放大镜
  - **法官**：圆形头 + 坐姿 + 拿法槌
  - **书记员**：圆形头 + 低头打字

**SVG 模板示例（控方律师）**：
```svg
<svg viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">
  <!-- 头 -->
  <circle cx="32" cy="14" r="8" fill="currentColor" />
  <!-- 身体 -->
  <rect x="22" y="22" width="20" height="22" rx="2" fill="currentColor" />
  <!-- 左脚 (走路动画分离) -->
  <rect x="24" y="44" width="6" height="16" rx="1" class="silhouette-leg-left" fill="currentColor" />
  <!-- 右脚 (走路动画分离) -->
  <rect x="34" y="44" width="6" height="16" rx="1" class="silhouette-leg-right" fill="currentColor" />
  <!-- 手指控方 -->
  <rect x="14" y="20" width="3" height="14" rx="1" class="silhouette-arm-pointing" fill="currentColor" />
</svg>
```

### 1.3 颜色方案（占位 + CSS var）

```css
/* frontend/styles/silhouettes.css */
:root {
  --silhouette-prosecutor: #B53A2E;     /* 绛红 */
  --silhouette-defender: #2C5470;       /* 深青 */
  --silhouette-investigator: #7A5C3F;   /* 棕金 */
  --silhouette-judge: #5C564F;          /* 深灰 */
  --silhouette-clerk: #A89F8E;          /* 米褐 */
}

.silhouette-prosecutor  { color: var(--silhouette-prosecutor); }
.silhouette-defender    { color: var(--silhouette-defender); }
.silhouette-investigator { color: var(--silhouette-investigator); }
.silhouette-judge       { color: var(--silhouette-judge); }
.silhouette-clerk       { color: var(--silhouette-clerk); }
```

**后期调整**：改 CSS var 即可全局换色，不需改 SVG path。

### 1.4 CSS Keyframes（走路 / 点头 / 举手）

```css
/* 走路 — 双脚交替 (0.8s 循环) */
@keyframes silhouette-walk {
  0%   { transform: translateY(0) rotate(0deg); }
  25%  { transform: translateY(-2px) rotate(5deg); }    /* 左脚抬起 */
  50%  { transform: translateY(0) rotate(0deg); }
  75%  { transform: translateY(-2px) rotate(-5deg); }   /* 右脚抬起 */
  100% { transform: translateY(0) rotate(0deg); }
}

.silhouette-leg-left {
  animation: silhouette-walk 0.8s ease-in-out infinite;
  transform-origin: center top;
}
.silhouette-leg-right {
  animation: silhouette-walk 0.8s ease-in-out infinite reverse;
  transform-origin: center top;
}

/* 点头 — 身体微微点头 (1.2s 循环) */
@keyframes silhouette-nod {
  0%, 100% { transform: rotate(0deg); }
  50%      { transform: rotate(8deg); }
}

.silhouette-nod {
  animation: silhouette-nod 1.2s ease-in-out infinite;
  transform-origin: center bottom;
}

/* 举手指控 — 律师专属 (2s 循环 + 抬升) */
@keyframes silhouette-point {
  0%, 100% { transform: rotate(0deg) translateY(0); }
  50%      { transform: rotate(-15deg) translateY(-3px); }
}

.silhouette-arm-pointing {
  animation: silhouette-point 2s ease-in-out infinite;
  transform-origin: center bottom;
}

/* 调查员放大镜 — 旋转 */
@keyframes silhouette-magnify {
  0%, 100% { transform: rotate(0deg); }
  50%      { transform: rotate(20deg); }
}

.silhouette-magnifier {
  animation: silhouette-magnify 1.5s ease-in-out infinite;
  transform-origin: center center;
}

/* 法官敲锤 — 关键帧 */
@keyframes silhouette-gavel {
  0%, 100% { transform: rotate(0deg) translateY(0); }
  30%      { transform: rotate(-10deg) translateY(-4px); }  /* 举锤 */
  60%      { transform: rotate(10deg) translateY(0); }       /* 落锤 */
}

.silhouette-gavel {
  animation: silhouette-gavel 0.4s ease-in-out infinite;
  transform-origin: center bottom;
}
```

### 1.5 React 组件

```typescript
// frontend/components/courtroom/silhouettes/Silhouette.tsx

interface SilhouetteProps {
  agentType: AgentType;
  isSpeaking?: boolean;
  isThinking?: boolean;
  isSearching?: boolean;     // 调查员专属
  isJudging?: boolean;       // 法官专属
  size?: number;             // px, default 64
}

export function Silhouette({
  agentType,
  isSpeaking,
  isThinking,
  isSearching,
  isJudging,
  size = 64,
}: SilhouetteProps) {
  return (
    <div
      className={`
        silhouette-${agentType}
        ${isSpeaking ? 'animate-speak' : ''}
        ${isThinking ? 'animate-think' : ''}
        ${isSearching ? 'animate-search' : ''}
        ${isJudging ? 'animate-judge' : ''}
      `}
      style={{ width: size, height: size }}
    >
      <ProsecutorSvg />
      {/* ... 等 5 个 SVG */}
    </div>
  );
}
```

```typescript
// frontend/components/courtroom/silhouettes/RoleSilhouette.tsx

export function RoleSilhouette({ agent, ...props }: RoleSilhouetteProps) {
  const Comp = {
    prosecutor: ProsecutorSilhouette,
    defender: DefenderSilhouette,
    investigator: InvestigatorSilhouette,
    judge: JudgeSilhouette,
    clerk: ClerkSilhouette,
  }[agent.agent_type] ?? ProsecutorSilhouette;

  return <Comp {...props} />;
}
```

### 1.6 AgentAvatar 替换

```typescript
// frontend/components/courtroom/AgentAvatar.tsx (MODIFIED)
import { RoleSilhouette } from "./silhouettes/RoleSilhouette";

// 替换圆形头像:
<div className="w-12 h-12 rounded-full bg-prosecution ...">  // OLD
<RoleSilhouette agent={agent} size={48} isSpeaking={isSpeaking} />  // NEW
```

**保持向后兼容**：
- 圆形 fallback（如果 SVG 加载失败）
- 气泡 / 调查员 spinner 等装饰不变
- `JudgeBiasMeter` 等下游组件不变

---

## 2. PR 拆分

### PR-D1: SVG 剪影组件 (commit `...`)

**目标**: 5 角色 SVG + 颜色 CSS var

**改动**:
- `frontend/components/courtroom/silhouettes/Silhouette.tsx` (NEW, ~150 行)
- `frontend/components/courtroom/silhouettes/ProsecutorSilhouette.tsx` (NEW)
- `frontend/components/courtroom/silhouettes/DefenderSilhouette.tsx` (NEW)
- `frontend/components/courtroom/silhouettes/InvestigatorSilhouette.tsx` (NEW)
- `frontend/components/courtroom/silhouettes/JudgeSilhouette.tsx` (NEW)
- `frontend/components/courtroom/silhouettes/ClerkSilhouette.tsx` (NEW)
- `frontend/components/courtroom/silhouettes/RoleSilhouette.tsx` (NEW, router)
- `frontend/styles/silhouettes.css` (NEW, ~80 行 — 颜色 var + keyframes)
- `frontend/tailwind.config.js` (MODIFIED, 加 silhouette-* color tokens)

**触及裁决**: **0**（纯视觉替换）

### PR-D2: CSS keyframes + React 集成 (commit `...`)

**目标**: 走路 / 点头 / 举手 / 敲锤 4 类动作 + AgentAvatar 接入

**改动**:
- `frontend/components/courtroom/silhouettes/SilhouetteAnimations.tsx` (NEW, ~80 行 — CSS class 应用)
- `frontend/components/courtroom/AgentAvatar.tsx` (MODIFIED — 圆形 → RoleSilhouette)
- `frontend/components/courtroom/AgentAvatar.test.tsx` (MODIFIED — 3 个新 sub-test)
- `frontend/components/courtroom/silhouettes/Silhouette.test.tsx` (NEW, 4 sub-test)
- `frontend/styles/silhouettes.css` (MODIFIED — 加走路/点头/举手/敲锤 keyframes)

**触及裁决**: **0**（纯视觉）

### PR-D3: release notes + ADR + tag (commit `...`)

**目标**: v2.0 发版文档 + tag

**改动**:
- `docs/release-notes/v2.0.md` (NEW, 14 章节模板) — 注意是 v2.0 不是 v1.0.5
- `docs/adr/0033-silhouette-architecture.md` (NEW)
- `docs/adr/README.md` (MODIFIED)
- `docs/V1-ROADMAP.md` (MODIFIED, M4 v2.0 ✅)
- `docs/decisioncourt-roadmap.md` (MODIFIED)

---

### PR-D3.5: 视觉打磨 + 兼容性 (commit `...`) ⏸ **v2.0 新增**

**目标**: 剪影小人视觉打磨 + 与 v1.0.x 圆形头像向后兼容（旧头像保留作为 fallback）

**改动**:
- `frontend/components/courtroom/silhouettes/SilhouetteRefinement.tsx` (NEW, ~80 行 — 微调: 走路节奏 / 点头幅度 / 颜色对比度 / 角色差异化)
- `frontend/components/courtroom/SilhouetteAvatar.tsx` (NEW, ~60 行 — 包装组件，提供 `mode="silhouette" | "circle"` 切换，向后兼容)
- `frontend/components/courtroom/AgentAvatar.tsx` (MODIFIED — 默认 silhouette mode，env var `NEXT_PUBLIC_USE_CIRCLE_AVATAR=true` 回退到圆形)
- `frontend/components/courtroom/SilhouetteRefinement.test.tsx` (NEW, 5 sub-test — 视觉回归测试)
- `frontend/components/courtroom/SilhouetteAvatar.test.tsx` (NEW, 3 sub-test — 兼容性测试)
- `docs/decisioncourt-ux-refinement.md` (MODIFIED — 加剪影小人视觉规范章节)

**触发条件**: PR-D2 完成后用户跑 1 周试用，收集视觉反馈后再启动 PR-D3.5。如果反馈正面可跳过。

**触及裁决**: **0**（纯视觉 + 兼容性）

**为何 v2.0 单独 PR 而不是 D2 一起做**:
- 用户反馈周期：动画效果必须用户多轮确认才能定稿
- 破坏性变更：旧用户 v1.0.x 看圆形头像，新用户 v2.0 看剪影，需要兼容层
- 调试周期：CSS keyframes 在不同浏览器/分辨率下表现可能不同，需要单独打磨窗口

---

## 3. 工作量估算

| PR | 内容 | v1.0.5 估 | v2.0 估 | 差异原因 |
|---|---|---|---|---|
| **PR-D1** | SVG 剪影 + 颜色 var | 0.5 周 | 0.5 周 | 不变 |
| **PR-D2** | CSS keyframes + AgentAvatar 接入 | 0.5 周 | 0.5 周 | 不变 |
| **PR-D3** | release notes + ADR + tag | 0.5 天 | 0.5 天 | 不变 |
| **PR-D3.5** | 视觉打磨 + 兼容性 | — | **1 周** ⏸ 新增 | v2.0 升格后用户反馈迭代窗口 |
| **总计** | | **1-1.5 周** | **2-3 周** | **+1 周**（打磨） |

---

## 4. 测试累计

| 包 | v1.0.4 | v2.0 新增 | 累计 |
|---|---|---|---|
| `frontend/components/courtroom/` | — | +3 (AgentAvatar test) +4 (Silhouette test) +5 (Refinement test) +3 (Avatar compat test) = **+15** | +15 |
| 其他 | 不变 | 不变 | 不变 |
| **总计 Go** | 309 | 0 | **309** |
| **总计 Frontend** | 56 | **+15** | **71** |

**v1.0.5 vs v2.0 测试覆盖差异**：
- v1.0.5: 7 sub-test（AgentAvatar 3 + Silhouette 4）
- v2.0: 15 sub-test（多 Refinement 5 + Avatar compat 3 — 视觉打磨 + 兼容性）

---

## 5. 风险与缓解（v2.0 升级版）

| 风险 | 影响 | 缓解 |
|---|---|---|
| SVG 在小尺寸看不清细节 | 视觉质量差 | viewBox 96x96 + scale 适配 |
| CSS keyframes 在低性能设备卡顿 | 庭审卡顿 | `will-change: transform` 启用 GPU + 减少 reflow |
| 5 个角色 SVG 不一致 | 视觉割裂 | 统一 viewBox + 统一头部比例 |
| 颜色后期调整涉及所有 SVG | 改色困难 | CSS var 解耦（fill="currentColor"） |
| 走路动画遮挡文字气泡 | UX 差 | 用 `motion.div` 调 y 偏移 ≤ 3px |
| **v2.0 新增** 用户不喜欢剪影风格 → 期望看回圆形 | 破坏性变更 | env var `NEXT_PUBLIC_USE_CIRCLE_AVATAR=true` 回退；组件 `mode="silhouette"\|"circle"` 切换 |
| **v2.0 新增** 视觉打磨窗口长（动画效果需多轮反馈） | 周期超预期 | PR-D3.5 独立 PR，用户 1 周试用后再启动 |
| **v2.0 新增** 旧 v1.0.x 用户升级看到剪影 | UX 突变 | 默认 silhouette mode，可手动回退；PR-D3 release notes 明确提示 |

---

## 6. 回滚策略

| PR | 回滚命令 |
|---|---|
| PR-D1 | `git revert <commit>` + silhouettes/ 目录删除 |
| PR-D2 | `git revert <commit>` + AgentAvatar 恢复圆形 |
| PR-D3 | `git reset --hard v1.0.4` 回 v1.0.4 |

---

## 7. 用户后续动作

1. **审核本规划文档** + ADR 0033
2. **审核 v1.0.4 Framer Motion 基础设施**（v1.0.5 复用）
3. **授权 PR-D1**（SVG 剪影组件）
4. PR-D1 完成后在浏览器看 5 个角色剪影 + 调色
5. PR-D2 完成后看走路 / 点头 / 举手动画
6. v1.0.5 tag + push

---

## 8. 不做什么（明确边界）

- ❌ **不引入 Lottie / Rive** —— 用户明确"不需要细节"
- ❌ **不做设计师立绘** —— 厕所标识剪影够用
- ❌ **不做 3D / R3F** —— 用户明确"2.5D"是 2D 立绘 + 透视感
- ❌ **不做 Live2D 风格的复杂互动** —— 剪影 + CSS keyframes 够
- ❌ **不做 per-role 独立动画配置** —— 5 角色共用 CSS keyframes（差异化通过 className）

---

## 9. 关联文档

- [V1.0.3-PLAN.md](./V1.0.3-PLAN.md) (Prompt Lab 前置)
- [V1.0.4-PLAN.md](./V1.0.4-PLAN.md) (Framer Motion 微动效前置)
- [V1-ROADMAP.md](./V1-ROADMAP.md) M4 v2.0 ✅
- [v1.0.4 release notes](./release-notes/v1.0.4.md) (前置)
- [PRD §5 UX 规范](../decisioncourt-ux-refinement.md) (v2.0 视觉对齐)
- [Tech Spec §11.4 前端规范](../decisioncourt-tech-spec.md) (Tailwind CSS 配置)
- [V3.0-PLAN.md](./V3.0-PLAN.md) (后续：端侧 TTS，v2.0 完成后启动)
- 历史: [V1.0.5-PLAN.md](git log V1.0.5-PLAN.md)（已 git mv 升级为 V2.0-PLAN.md）