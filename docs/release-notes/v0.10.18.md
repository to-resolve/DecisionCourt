# v0.10.18 Release Notes — 安全审计 P0 阶段收尾

| | |
|---|---|
| **版本** | v0.10.18 |
| **发布日期** | 2026-07-12 |
| **状态** | ✅ 已部署 ECS（2026-07-12，镜像 tag `v0.10.18`，ECS `47.239.152.177` 实际运行 24 天无重启） |
| **Commit** | `705bb12` |
| **Tag** | `v0.10.20`（实际镜像 `:latest` 标签层级：v0.10.17 ← v0.10.18 ← v0.10.20） |
| **触发事件** | v0.10.17 silent-error-fix 收尾后,启动安全审计 [security-audit-2026-07-03.md](../../.trae/documents/security-audit-2026-07-03.md) P0 阶段 |
| **完整记录** | [ADR 0025 §1-§7](../adr/0025-security-p0-closeout.md) |
| **回滚** | `git push origin v0.10.17`（前一可用 tag）|

---

## 🎯 核心主题

**完成安全审计 P0 阶段最后 2 项修补，让 JWT 鉴权真正"非默认密钥" + 容器 UID 10001 符合 K8s 惯例。**

---

## 📊 影响指标

| 维度 | v0.10.17 | v0.10.18 | Δ |
|---|---|---|---|
| JWT_SECRET 默认值（伪安全陷阱）| "decisioncourt-secret" | **不设**（fail-fast）| 100% 修复 |
| 容器 UID | 1001 | **10001**（K8s 惯例）| 升级 |
| OWASP A01（访问控制）| 基本达标 | ✅ 达标 | — |
| OWASP A02（加密失败）| 部分达标 | ✅ 达标 | — |
| OWASP A05（安全配置）| 部分达标 | ✅ 达标 | — |
| OWASP A07（认证失败）| 已实装 | ✅ 已实装 | — |
| K8s PSP/PSS restricted | ❌ 拒 | ✅ 过 | 通过 |
| OpenShift restricted SCC | ❌ 拒 | ✅ 过 | 通过 |

---

## 🐛 修复清单

### P0-2: JWT_SECRET 默认值移除（A02 + A05）

**漏洞**：

```go
// backend/internal/config/config.go (修复前)
viper.SetDefault("JWT_SECRET", "decisioncourt-secret")
```

让 `Load()` 的 `mustEnvs` fail-fast 检查形同虚设——用户即使忘了设 JWT_SECRET，程序也照常启动，所有 JWT 都用公开仓库能读到的密钥签发。

**修复**：

```diff
- viper.SetDefault("JWT_SECRET", "decisioncourt-secret")
+ // 不设默认值,缺失时由 Load() 的 mustEnvs log.Fatalf 阻断启动
```

**部署配置配套**：

```yaml
# docker-compose.yml L117 (v0.8.3 已有)
JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set in .env}
```

---

### P0-3: 容器 UID 1001 → 10001（A05）

**漏洞**：4 处配置使用 UID 1001，与某些发行版系统用户冲突，且不符合 K8s 部署惯例。

**修复**：

| 文件 | 修改 |
|---|---|
| `backend/Dockerfile` | `addgroup -g 10001 -S appgroup` + `adduser -u 10001 -S appuser -G appgroup` |
| `frontend/Dockerfile` | `addgroup -g 10001 -S nodejs` + `adduser -u 10001 -S nextjs -G nodejs` |
| `docker-compose.yml` backend | `user: "10001:10001"` |
| `docker-compose.yml` frontend | `user: "10001:10001"` |
| `docker-compose.yml` 顶部注释 | `1001:1001` → `10001:10001` |

---

## 🧪 测试

### 单元测试

```
ok      github.com/decisioncourt/backend/internal/config  2.521s
ok      github.com/decisioncourt/backend/internal/auth    4.895s
```

### 编译

```
$ go build ./...
(no errors)
```

---

## 🔧 修改文件（4 个）

| 文件 | 改动 |
|---|---|
| `backend/internal/config/config.go` | -1 / +6（删除 default + 注释说明）|
| `backend/Dockerfile` | -2 / +2（UID 1001 → 10001）|
| `frontend/Dockerfile` | -2 / +2（UID 1001 → 10001）|
| `docker-compose.yml` | -5 / +5（4 处 UID + 1 处注释）|

总计：-10 / +15 行

---

## 📅 时间线

```
2026-07-03  安全审计报告 v1.0 写完
2026-07-12  v0.10.17 silent-error-fix 收尾 + 用户授权启动 P0 阶段
2026-07-12  P0 现状盘点：6 项中 4 项 v0.8.3 已实装,剩 2 项
2026-07-12  P0-2: 删除 config.go JWT_SECRET SetDefault
            P0-3: 4 处 UID 1001 → 10001
            go build + go test 通过
2026-07-12  commit 705bb12 "security: P0-2 + P0-3 安全加固 (v0.10.18)"
2026-07-12  ADR 0025 写完 + commit 38cf860
2026-07-12  git push origin main + git tag v0.10.18 + git push origin v0.10.18
            🟡 GitHub Actions Test + Deploy 触发中
2026-07-12  ⏸ v0.10.18 部署中 (预计 5-6 分钟)

---

## 📝 反思（2026-08-05 retrospective）

**文档 vs 实际部署时间差：24 天**

2026-08-05 30 天生产沉淀时发现：本 release notes 发布时写"⏸ v0.10.18 部署中 (预计 5-6 分钟)"，但 24 天后（2026-08-05）ECS 实际状态是 `Up 3 weeks (healthy)`，`/opt/DecisionCourt/logs/backend/` 仍是空目录、`main.go:223` 启动日志每天 24h 都声称自己是 `v0.9.2`，说明 v0.10.18 早已成功部署但 release notes 状态栏从未更新。

**为什么会出现这种错位**：见 [production-retrospective-2026-08-05.md §5 关键里程碑时间线（29 天）](../deployment/production-retrospective-2026-08-05.md#5-关键里程碑时间线29-天)。

**修复**：本 release notes 状态栏已更新为"✅ 已部署 ECS（2026-07-12 ... 实际运行 24 天无重启）"。同一份错位发生在 v0.10.20.md，已一并修正。v0.10.17.md 状态栏在最初发布时就已写"✅ 已部署 ECS"，是唯一无错位的版本。

**教训**（写入 [AGENTS.md §1.2 文档一致性](../../AGENTS.md)）：发布流程的最后一步应包含"部署完成 → 更新 release notes 状态 → close 文档 ticket"，而不是"git push tag → 自认为结束"。本次修复同步把这条反思写入 v0.10.20.md。
```

---

## 🛡 部署后验证 checklist

| # | 项 | 期望 |
|---|----|------|
| 1 | 容器内 `id` 命令 | `uid=10001(appuser) gid=10001(appgroup)` |
| 2 | `JWT_SECRET` 未设时启动 | 立即退出,日志 `FATAL: required config JWT_SECRET is empty` |
| 3 | `JWT_SECRET` 已设时启动 | 正常 |
| 4 | 浏览器访问 `/api/v1/sessions` 无 cookie | 401 error code 1401 |
| 5 | `/api/v1/auth/anon` 返回 JWT + Set-Cookie | ✅ |

---

## 📋 P0 阶段全景（6 项全部 ✅）

| # | 项 | 阶段 | Commit |
|---|----|------|--------|
| **P0-1** | 全栈完全无鉴权 | v0.8.3 | — |
| **P0-2** | JWT_SECRET 默认值 | **v0.10.18** | `705bb12` |
| **P0-3** | 容器以 root 跑 | **v0.10.18** | `705bb12` |
| **P0-4** | 关键 env 无 fail-fast | v0.8.3 | — |
| **P0-5** | SubmitEvidence 任意用户可写 | v0.8.3 | — |
| **P0-6** | Export 无鉴权 + 无审计 | v0.8.3 | — |

---

## ⏸ 已知遗留 / 下一步

| 项 | 原因 | 计划 |
|----|------|------|
| **安全 P1 阶段** | WS Origin 兜底 / CSRF / 限流 / 输入校验 / 日志脱敏 / 依赖固定 / dev→prod | 1-2 周（[security-audit-2026-07-03.md §3](../../.trae/documents/security-audit-2026-07-03.md)）|
| **安全 P2 阶段** | HTTP 安全头 / SSRF SeArxNG / CSP / .git 排除 | 1 季度 |
| **Phase A 真实庭审** | 5-10 场真实庭审采集增点数据 | 1-2 周 |

---

## 🔗 关联文档

- [ADR 0025 §1-§7](../adr/0025-security-p0-closeout.md) — 完整设计决策记录
- [security-audit-2026-07-03.md](../../.trae/documents/security-audit-2026-07-03.md) — 安全审计原始报告
- [ADR 0024](../adr/0024-silent-error-fix-pr1.md) — v0.10.17 静默错误修复
- [release-notes/v0.10.17.md](./v0.10.17.md) — 前一版本发布说明

---

## 🙏 致谢

感谢 2026-07-03 的安全审计报告，把 20 个安全问题（6 P0 + 7 P1 + 5 P2 + 2 P3）一次性梳理清楚。本次发版完成了 6 个 P0 中的最后 2 项，让 A01/A02/A05/A07 风险从"严重不达标"升至"达标"。