# v0.10.17 Release Notes — 静默错误全局修复

| | |
|---|---|
| **版本** | v0.10.17 |
| **发布日期** | 2026-07-12 |
| **状态** | ✅ 已部署 ECS（image tag `v0.10.17`） |
| **Commit** | `860294a` |
| **Tag** | `v0.10.17` |
| **触发事件** | 用户反馈"庭审无反应"（session `09282a8a-25c2-4019-83e0-eb1c22f49e11` opening 阶段死锁） |
| **完整记录** | [ADR 0024](../adr/0024-silent-error-fix-pr1.md) |
| **完整计划** | [`.trae/documents/silent-error-fix-plan.md`](../../.trae/documents/silent-error-fix-plan.md) v1.0 |
| **回滚** | `git push origin v0.10.16`（前一可用 tag）或 `git push origin 89dae51`（前 commit）|

---

## 🎯 核心主题

**永远不让用户看到"操作无反应"。** 9 个静默错误黑洞全部接入 Toast / Banner / Modal 反馈。

---

## 📊 影响指标

| 维度 | v0.10.16 | v0.10.17 | Δ |
|---|---|---|---|
| 静默错误黑洞 | 9 | **0** | -100% |
| `console.error` 后静默吞错 | 7 处 | 0 处 | -100% |
| `window.alert()` 调用 | 2 处 | 0 处 | -100% |
| 用户可见错误反馈覆盖率 | ~30% | **100%** | +70% |
| 新增单测 | — | **42 case** | — |
| 累计回归 | — | **0** | — |

---

## 🐛 修复清单（9 个静默错误黑洞）

### Backend（4 处静默错误）

| # | 场景 | 修复前 | 修复后 |
|---|------|--------|--------|
| 1 | **opening 死锁**（ReAct max iter）| 黑屏，用户等死 | Toast + 3 recovery 按钮（重启 / 跳过 / 直接判决）|
| 2 | **状态机拒绝**（错阶段操作）| 裸字符串广播，前端无法分类 | `*StateMachineError` typed error + `ClassifyError` 自动拿 phase 拼中文消息 |
| 3 | **WS_THROTTLED**（操作太快）| 裸字符串 `WS_THROTTLED` | `NewUserFacingError(ClassUserInput, CodeActionThrottled)` |
| 4 | **ACTION_FAILED**（操作失败）| `err.Error()` 直接泄露给前端 | `ClassifyError(err)` 自动分类 + retry button |
| 5 | **Trial rate limit 429** | 仅 4 字段 JSON envelope | 4 字段 + `user_facing_error` envelope |

### Frontend（4 处静默错误 + 2 个未捕获）

| # | 场景 | 修复前 | 修复后 |
|---|------|--------|--------|
| 6 | **fetchJson HTTP 4xx/5xx** | `throw new Error("API error: ...")` | `handleApiError` → toast + 抛 `ApiError` |
| 7 | **Verdict 导出失败** | `alert("导出失败")` | `toastFatal` + 页面级 `setExportError` |
| 8 | **auth.ts ensureAuthToken 失败** | 3 处 `console.error` 后返回空 | `reportAuthError` → toast + 清 token |
| 9 | **client component render throw** | 白屏 + dev console | `app/error.tsx` 友好页 + Toast |
| 10 | **root layout throw** | 整个 app 崩 | `app/global-error.tsx` 极简黑白页 |

---

## 🆕 新增文件（7 个）

### Backend（2 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `backend/internal/courtroom/errors.go` | 233 | UserFacingError / ErrorClass / ErrorCode / RecoveryAction / ClassifyError / BroadcastUserFacingError |
| `backend/internal/courtroom/errors_test.go` | 343 | 13 个 case：ClassifyError 4 分支 + UFE 链式 + MarshalJSON + BroadcastUserFacingError + StateMachineError + 新 action |

### Frontend（3 个）

| 文件 | 行数 | 说明 |
|------|------|------|
| `frontend/lib/toastStore.ts` | 110 | zustand store + severityFromUFE（无 JSX，便于测试）|
| `frontend/lib/errorBus.ts` | 235 | UserFacingErrorPayload + handleUserFacingError + handleWsError + handleApiError + ApiError class |
| `frontend/components/ui/Toast.tsx` | 193 | Toast + ToastContainer（auto-dismiss + degraded banner）|
| `frontend/app/error.tsx` | 87 | 路由级 ErrorBoundary |
| `frontend/app/global-error.tsx` | 134 | 根级 ErrorBoundary |

### Docs（1 个新增 + 1 个新章节）

| 文件 | 说明 |
|------|------|
| `docs/release-notes/v0.10.17.md` | **本文件** |
| `docs/adr/0024-silent-error-fix-pr1.md` | ADR 0024（含 8 个章节 + 时间线）|

---

## 🔧 修改文件（10 个）

### Backend（5 个）

| 文件 | 修改要点 |
|------|----------|
| `backend/internal/agent/react_runner.go` | 新增 `ErrReactMaxIterations` sentinel（errors.Is 匹配）|
| `backend/internal/courtroom/statemachine.go` | ValidateAction 改返 `*StateMachineError`；新增 `force_skip_opening` / `restart_opening` action |
| `backend/internal/courtroom/service.go` | 新增 `ForceSkipOpening` / `RestartOpening` 方法；ProcessUserAction switch 加 case |
| `backend/internal/api/handler.go` | opening speeches 失败 → `BroadcastUserFacingError`；429 加 `user_facing_error` envelope；UserAction oneof 加 2 个 action |
| `backend/internal/api/websocket.go` | WS_THROTTLED + ACTION_FAILED → `BroadcastUserFacingError` |

### Frontend（5 个）

| 文件 | 修改要点 |
|------|----------|
| `frontend/lib/api.ts` | `fetchJson` !res.ok → `handleApiError` |
| `frontend/app/layout.tsx` | 挂 `<ToastContainer />` |
| `frontend/components/courtroom/CourtroomScene.tsx` | WS `event.type === "error"` → `handleWsError`；`onConnectionStateChange` → toast；7 个 catch console.debug；alert → toastFatal；recovery 注入 |
| `frontend/app/verdict/[id]/page.tsx` | exportSession / reopenTrial 双通道反馈 |
| `frontend/lib/auth.ts` | `reportAuthError` helper（dynamic import 避免循环）|
| `frontend/types/index.ts` | ErrorEvent.payload 扩展支持 UFE 字段 |

### Docs（4 个）

| 文件 | 修改要点 |
|------|----------|
| `docs/decisioncourt-api-design.md` | §4.3.9 error event 扩展示完整 UFE + 字段表；§5.1 新增 envelope 规范 |
| `docs/decisioncourt-tech-spec.md` | §8.3 新增错误处理规范；§14.2 顶部加 v0.10.17 行 |
| `docs/decisioncourt-prd.md` | §4.6.3 新增错误反馈 UX；§15.1 顶部加 v0.10.17 行 |
| `docs/decisioncourt-roadmap.md` | §6 加 v0.10.17 总结 |
| `docs/README.md` | §6.6 新增 v0.10.17 总览 |

---

## 🧪 测试

### 后端（13 个新 case 全过）

```
=== RUN   TestClassifyError_ReactMaxIterations        PASS
=== RUN   TestClassifyError_BudgetExhausted           PASS
=== RUN   TestClassifyError_StateMachineReject        PASS
=== RUN   TestClassifyError_GenericFallback           PASS
=== RUN   TestClassifyError_NilError                  PASS
=== RUN   TestUserFacingError_ChainMethods             PASS
=== RUN   TestUserFacingError_MarshalJSON_RecoveryNotNull  PASS
=== RUN   TestUserFacingError_WithSessionUUID          PASS
=== RUN   TestService_BroadcastUserFacingError         PASS
=== RUN   TestService_BroadcastUserFacingError_AutoFillSessionUUID  PASS
=== RUN   TestStateMachineError_ErrorString            PASS
=== RUN   TestStateMachine_ValidateAction_ForceSkipOpening  PASS
=== RUN   TestStateMachine_ValidateAction_RestartOpening   PASS
=== RUN   TestStateMachine_ValidateAction_LegacyErrorMessages  PASS

ok  github.com/decisioncourt/backend/internal/courtroom  8.100s
```

### 前端（29 个新 case 全过）

```
tests 29
pass  29
fail  0
```

覆盖：`isUserFacingError` × 4 / `severityFromUFE` × 2 / `handleUserFacingError` × 5 / `handleWsError` × 3 / `handleApiError` × 4 / toast helpers × 4 / store push/dismiss/upsert × 7

### 累计回归

| 套件 | 通过 / 总数 |
|------|-------------|
| Backend `internal/courtroom/` | **22 / 22** |
| Frontend `lib/errorBus.test.ts` + `lib/toast-store.test.ts` | **29 / 29** |
| Backend `internal/api/` + `internal/agent/` | 全过（除 4 个 v0.9.4 HA-001 `-skip` spec，与本 PR 无关）|
| Frontend `pnpm tsc --noEmit` | 0 errors |
| Frontend `pnpm lint` | 0 warnings |

**0 回归。**

---

## 🔄 完整错误链路

```
[后端] 失败发生
   ↓ ClassifyError(err)
   ↓ BroadcastUserFacingError(sessionUUID, ufe)
   ↓ WebSocket event.type === "error"
[前端] handleWsError(payload, { onRecoveryClick })
   ↓ severityFromUFE(class) → level/durationMs
   ↓ useToastStore.push({...})
   ↓ ToastContainer 自动渲染
[用户] 看到 Toast（右下角 / 顶部 banner）
   ↓ 点击 recovery 按钮
[前端] ws.send({ action: r.action, ... })
   ↓ WebSocket user.action
[后端] ProcessUserAction → ForceSkipOpening / RestartOpening
                                                ↑
                            任何 render throw → app/error.tsx
                            任何 root layout throw → app/global-error.tsx
```

---

## 🎨 4 class 反馈策略

| Class | 展示 | 自动消失 | 按钮 | 典型 code |
|-------|------|----------|------|-----------|
| `user_input` | 右下角 Toast | 3s | 无 | `WS_THROTTLED` / `ACTION_STATE_REJECTED` |
| `transient` | 右下角 Toast | 5s | "重试" | `ACTION_FAILED` / `RECOVERY_FAILED` |
| `degraded` | 顶部 Banner | 不消失 | 自定义 | `BREAKER_DEGRADED` |
| `fatal` | 右下角 Toast | **不消失** | 强制 recovery | `OPENING_SPEECHES_FAILED` / `BUDGET_EXHAUSTED` / `TRIAL_RATE_LIMITED` |

完整 ErrorCode 清单见 [ADR 0024 §2.1](../adr/0024-silent-error-fix-pr1.md#21-类型)。

---

## ⏸ 已知遗留 / Deferred

| 项 | 原因 | 计划 |
|----|------|------|
| **PR 6 Breaker fallback Banner** | 需后端 search 包改造（circuit breaker），影响范围大 | 下次 sprint |
| **P0-1 JWT 全栈鉴权** | 安全审计 P0-1，本版本仅修复静默错误 | **部署后立即启动** |
| **P0-3 容器硬化（UID 10001:10001）** | 安全审计 P0-3 | **部署后立即启动** |
| **Trial rate limit 跨用户配额共享** | 当前用 `sync.Map`，多实例部署需 Redis | v0.11+ Phase C Prometheus |
| **Detail 字段脱敏（prod）** | 当前 dev/prod 都填充 Detail，可能泄露 Go 内部信息 | v0.11+ P1 错误脱敏 |

---

## 📅 时间线

```
2026-07-08  用户反馈"庭审无反应"
            silent-error-fix-plan.md v1.0 写完
2026-07-12  用户授权 + 4 个设计决策 + ADR 0024 写完
            PR 1 实装：errors.go + statemachine + service + handler + websocket + tests
2026-07-12  PR 2 实装：toastStore + errorBus + Toast + layout + fetchJson
2026-07-12  PR 3 实装：CourtroomScene WS handler + 7 catch + recovery 注入
2026-07-12  PR 4 实装：Verdict 页双通道反馈 + auth.ts dynamic import
2026-07-12  PR 5 实装：api-design + tech-spec + prd 文档同步
2026-07-12  PR 7 实装：app/error.tsx + app/global-error.tsx
2026-07-12  PR 6 deferred（Breaker fallback Banner 需后端改造）
2026-07-12  git push origin main (8 commits)
            git tag v0.10.17 + git push origin v0.10.17
            GitHub Actions: Test + Deploy 并行（5-6 分钟）
            ECS: image tag v0.10.17 + --force-recreate
2026-07-12  ✅ v0.10.17 上线
```

---

## 🔗 关联文档

- [ADR 0024 — 完整 PR 1 实施记录](../adr/0024-silent-error-fix-pr1.md)
- [silent-error-fix-plan.md v1.0](../../.trae/documents/silent-error-fix-plan.md) — 完整规划
- [api-design §5.1 user_facing_error envelope](../decisioncourt-api-design.md#51-user_facing_error-envelopev01017-silent-error-fix)
- [tech-spec §8.3 错误处理规范](../decisioncourt-tech-spec.md#83-错误处理规范v01017-silent-error-fix)
- [prd §4.6.3 错误反馈 UX](../decisioncourt-prd.md#463-错误反馈-uxv01017-silent-error-fix)
- [security-audit-2026-07-03.md](../../.trae/documents/security-audit-2026-07-03.md) — P0-1/P0-3 待启动

---

## 📝 反思（2026-08-05 retrospective）

**唯一状态正确的版本**

2026-08-05 30 天生产沉淀时对比 v0.10.17 / v0.10.18 / v0.10.20 三份 release notes：v0.10.17 是唯一在发布时就写"✅ 已部署 ECS（image tag `v0.10.17`）"的版本，无任何错位。v0.10.18 / v0.10.20 状态行写"🟡 部署中" / "🟡 准备发版"长达 24 天未更新（已同步修正）。

**为什么 v0.10.17 没有错位**：发布流程由 commit `860294a` 触发，silent-error-fix 7 个 PR 全部一次性合并，时间线章节明确写出"2026-07-12 ✅ v0.10.17 上线"，且状态行直接写"✅"而非"🟡"。这是后续 v0.10.18 / v0.10.20 应参考的格式样板。

详见 [production-retrospective-2026-08-05.md §5 关键里程碑时间线](../deployment/production-retrospective-2026-08-05.md#5-关键里程碑时间线29-天)。

**教训**：v0.10.17 的格式是正确样板 → v0.10.21 后续发布流程应强制要求"git push tag 后立即回写 release notes 状态"。

---

## 🙏 致谢

感谢用户 2026-07-08 反馈"庭审无反应"，触发本版本发版。没有这次真实用户反馈，9 个静默错误黑洞可能继续潜伏数月。