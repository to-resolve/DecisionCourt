# v0.10.20 Release Notes — 4 层限流防御深度（Defense in Depth）

| | |
|---|---|
| **版本** | v0.10.20 |
| **发布日期** | 2026-07-12 |
| **状态** | ✅ 已部署 ECS（2026-07-12，镜像 tag `v0.10.20`，ECS `47.239.152.177` 实际运行 24 天无重启） |
| **Commit** | `cbbaf3d`（实际部署 commit，非发布时"待 push"状态）|
| **Tag** | `v0.10.20` |
| **实际部署层级** | v0.10.17 ← v0.10.18 ← **v0.10.20**（当前 `:latest`）|
| **触发事件** | 用户 2026-07-12 需求："加一个可以在面试中写的后端亮点, 真正需要的而不是堆砌"。选定 4 层限流防御深度。 |
| **完整记录** | [ADR 0027](../adr/0027-rate-limit-defense-in-depth.md) + [interview/12](../interview/12-rate-limit-defense-in-depth.md) |
| **回滚** | `git push origin v0.10.19`（前一可用 tag）|

---

## 🎯 核心主题

**为 DecisionCourt 设计 4 层限流防御深度，从单点 L2 升级到全栈 defense in depth。每层用最合适算法 + 接入位置（HTTP vs 业务），覆盖 DDoS / 配额滥用 / F5 狂点 / 全局 OOM 4 类攻击向量。**

---

## 📊 影响指标

| 维度 | v0.10.19 | v0.10.20 | Δ |
|---|---|---|---|
| 限流层数 | 1 (Per-User Trial) | **4 (L0+L1+L2+L3)** | +3 |
| 限流算法数 | 1 (滑动窗口) | **4 (令牌桶/滑动窗口/令牌桶/信号量)** | +3 |
| 限流测试用例 | 0 | **19** (6 L1 + 6 L0 + 4 集成 + 3 metric) | +19 |
| 防 ECS OOM (5 trial × 400MB) | ❌ 无上限 | ✅ max=5 硬上限 | 100% 修复 |
| 防 F5 狂点 (1 秒 30 次 action) | ❌ 无保护 | ✅ RPS=2 + burst=5 | 100% 修复 |
| 防 LLM 配额烧光 (脚本刷 action) | ❌ session 内无限流 | ✅ Per-Session 限流 | 100% 修复 |
| Metric 名定义 (限流相关) | 0 | **4** (counter / gauge × 2) | +4 |

---

## 🆕 新增功能

### L0: 全局并发 trial 信号量（buffered channel）

**解决**：阿里云 ECS 2C2G 被 5 个 trial 同时跑挂 OOM（2026-07-12 已观察到 1 次）。

**实装**：
- `backend/internal/courtroom/concurrency.go`（[L0 ConcurrencyLimiter](file:///d:/源码/FullStack/DecisionCourt/backend/internal/courtroom/concurrency.go)）—— buffered channel 信号量，max=5
- 接入 `service.withCancel` 入口（**L0 只在 service 层 1 处管理**，避免双重 acquire bug）
- wrappedCancel 在 defer cancel 时自动 Release
- 4 个 metric（rejected_total / current / max）

**面试关键**：
- 信号量 vs 限流的区别：信号量表达"资源硬上限"，限流表达"频率"
- 为什么选 buffered channel：Go 习惯写法 + select default 自然表达非阻塞

### L1: Per-Session action 限流（令牌桶）

**解决**：用户 F5 狂点同一 session 的"继续质证"按钮（实测 1 秒 30 次 action），把正常用户拖慢 30 倍。

**实装**：
- `backend/internal/middleware/session_ratelimit.go`（[L1 SessionRateLimit](file:///d:/源码/FullStack/DecisionCourt/backend/internal/middleware/session_ratelimit.go)）—— 复用 `golang.org/x/time/rate`，RPS=2 / Burst=5
- 按 session UUID 隔离（map[string]*rate.Limiter + sync.Mutex）
- 接入 `handler.RegisterAPIRoutes` 内 `llmGroup.Use(h.SessionRateLimit)`
- OnReject 回调接入 `MetricSessionRateLimitRejectedTotal`

**面试关键**：
- 选令牌桶 vs 漏桶：允许突发（F5 狂点 5 次连续仍通过）+ 平滑长期流量
- L1 接 HTTP middleware 层 vs L0 接 service 层：HTTP 层立即返 429（用户体验好），service 层走 WS 通道（业务级错误）

### L2: Per-User Trial 限流（滑动窗口，已有 v0.9 ADR 0014）

**未改动**，仅作为 4 层防御的第 2 层。

### L3: Per-IP 通用限流（令牌桶，已有 v0.8.3）

**未改动**，仅作为 4 层防御的第 3 层。

---

## 🧪 测试

### 单元 + 集成测试（共 19 个新增）

```
L1 Per-Session middleware:
  PASS  TestSessionRateLimit_BasicAllowDeny
  PASS  TestSessionRateLimit_BurstThenRefill
  PASS  TestSessionRateLimit_DifferentSessionsIndependent
  PASS  TestSessionRateLimit_ConcurrentSafe
  PASS  TestSessionRateLimit_EmptySessionUUID
  PASS  TestSessionRateLimit_DefaultConfig

L0 ConcurrencyLimiter:
  PASS  TestConcurrencyLimiter_BasicAcquireRelease
  PASS  TestConcurrencyLimiter_ConcurrentSafe
  PASS  TestConcurrencyLimiter_Stats
  PASS  TestConcurrencyLimiter_ZeroMax
  PASS  TestConcurrencyLimiter_NilSafe
  PASS  TestConcurrencyLimiter_ReleaseWithoutAcquire

L0 + Service 集成:
  PASS  TestService_NilConcurrencyLimiter_NoLimit
  PASS  TestService_WithConcurrencyLimiter_Limits
  PASS  TestService_WithConcurrencyLimiter_FullCycle
  PASS  TestService_ConcurrencyLimiter_DefaultMax

L0 + Metric 集成:
  PASS  TestService_RecordConcurrencyMetric_Rejected
  PASS  TestService_RecordConcurrencyMetric_Gauges
  PASS  TestService_RecordConcurrencyMetric_NilSafe
```

### 编译

```
$ go build ./...
(no errors)
```

### 全包测试（除预存 agent 包 3 个 prompt 测试 fail）

```
ok      internal/agent_gateway        (15.987s)
ok      internal/api                  (17.174s)
ok      internal/auth                 (9.907s)
ok      internal/belief               (5.668s)
ok      internal/config               (4.579s)
ok      internal/courtroom            (13.195s) ← 含 16 个新测试
ok      internal/idempotency          (3.773s)
ok      internal/investigation        (5.888s)
ok      internal/middleware           (11.027s) ← 含 6 个新测试
ok      internal/observability        (15.212s)
ok      internal/private_memory       (5.655s)
ok      internal/ratelimit            (3.770s)
ok      internal/search               (7.317s)
ok      internal/util                 (3.612s)
FAIL    internal/agent                (预存 prompt 测试, 与本次无关)
```

**注**：`internal/agent` 3 个 prompt 测试 fail 是 v0.10.18 之前就存在的问题（git stash 掉本次改动后仍 fail），跟限流改动无关。属已知遗留，单独修复。

---

## 🐛 实施过程中的 2 个真实 bug（教训）

### Bug #1: handler 层 + service 层双重 acquire

**症状**：`TestService_WithConcurrencyLimiter_Limits` integration test fail —— 同 1 个 trial 占了 2 个 slot（max=5 实际只能开 2-3 个）。

**根因**：最初设计在 handler.StartTrial 入口直接 TryAcquire + service.withCancel 内部也 TryAcquire，同 1 个 trial 双 acquire。

**修复**：撤销 handler 层 TryAcquire，**L0 只在 service.withCancel 一处管理**。失败 UX 改为：HTTP 200 返回 → 后台 goroutine 失败 → WS 推送 `CodeConcurrentTrialLimit`。

**学习**：**限流器接入点选择 > 算法选择**。集成测试是唯一能暴露多层串扰的方式。

### Bug #2: Release 在空 channel 上阻塞 30s

**症状**：`TestConcurrencyLimiter_NoAcquireRelease_Panics`（预期 panic）实际 hang 30s 超时。

**根因**：`<-l.sem` 在空 channel 上是 receive 操作，**永久阻塞**。

**修复**：Release 改为 `select default` 非阻塞 + `slog.Warn`。生产环境优先避免进程卡死 30s。

**学习**：buffered channel 信号量的 Release 必须是宽容的（不阻塞 + log warning），不应 panic。

详见 [interview/12 §12.3](../interview/12-rate-limit-defense-in-depth.md) 完整 5 个教训。

---

## 🔧 修改文件（10 个）

| 文件 | 类型 | 行数 | 内容 |
|---|---|---|---|
| `backend/internal/middleware/session_ratelimit.go` | 新增 | ~140 | L1 middleware |
| `backend/internal/middleware/session_ratelimit_test.go` | 新增 | ~280 | 6 个 L1 测试 |
| `backend/internal/courtroom/concurrency.go` | 新增 | ~140 | L0 ConcurrencyLimiter |
| `backend/internal/courtroom/concurrency_test.go` | 新增 | ~210 | 6 个 L0 单元测试 |
| `backend/internal/courtroom/concurrency_integration_test.go` | 新增 | ~170 | 4 个 L0 + service 集成测试 |
| `backend/internal/courtroom/concurrency_metric_test.go` | 新增 | ~190 | 3 个 metric 接入测试 |
| `backend/internal/courtroom/service.go` | 修改 | +90 | withCancel 加 L0 + recordConcurrencyMetric + WithConcurrencyLimiter |
| `backend/internal/courtroom/errors.go` | 修改 | +25 | CodeConcurrentTrialLimit + ClassifyError 接入 |
| `backend/internal/api/handler.go` | 修改 | +35 | SessionRateLimit 字段 + L0 注释 |
| `backend/internal/observability/metrics.go` | 修改 | +12 | 4 个 metric 名常量 |
| `backend/cmd/server/main.go` | 修改 | +15 | 注入 L0 + L1 middleware |

**总计**：~1100 行（含测试），代码本身 ~310 行（测试:代码 ≈ 2.5:1）。

---

## 📅 时间线

```
2026-07-12  用户需求："加面试能写的后端亮点"
2026-07-12  调研 5 个方案 + 推荐主推方案（4 层限流防御深度）
2026-07-12  写 ADR 0027 设计文档 + interview/12 详细学习文档
2026-07-12  用户审阅 + 3 个拍板点确认（L1=RPS=2/Burst=5, L0=max=5, 4 个 unique code）
2026-07-12  PR 1: L1 Per-Session middleware + 6 测试 ✅
2026-07-12  PR 2: L0 ConcurrencyLimiter + 10 测试 + service.withCancel 集成 ✅
2026-07-12  集成测试捕获双重 acquire bug, 撤销 handler 层 TryAcquire
2026-07-12  PR 3: 4 个 metric 接入 + 3 个 metric 测试 ✅
2026-07-12  PR 4: ADR 0027 §7A 实施后差异 + interview/12 §12-15 实施过程 + 本 release-notes ✅
2026-07-12  commit `cbbaf3d` "feat(rate-limit): 4 层限流防御深度 (L3+L2+L1+L0), 19 测试 (v0.10.20)" + git tag v0.10.20 + push
            GitHub Actions: Test + Deploy 并行（5-6 分钟）
            ECS: image tag v0.10.20 + --force-recreate
2026-07-12  ✅ v0.10.20 上线

---

## 📝 反思（2026-08-05 retrospective）

**文档 vs 实际部署时间差：24 天**

2026-08-05 30 天生产沉淀时发现：本 release notes 发布时状态写"🟡 准备发版（PR 1-3 已合并, 待 commit + tag）"，commit 字段写"待 push（HEAD 6ed8c3f + PR 1-3 改动）"。但 24 天后（2026-08-05）ECS 实际状态是 `dc_backend Up 3 weeks (healthy)`，ECS 内存 18.95 MiB / 0 重启，commit `cbbaf3d` + tag `v0.10.20` 早已发布部署。

**为什么 release notes 没更新**：见 [production-retrospective-2026-08-05.md §5 关键里程碑时间线](../deployment/production-retrospective-2026-08-05.md#5-关键里程碑时间线29-天)。发布流程的最后一步是"git push tag"，但 push 完没有强制要求"回写 release notes 状态" → 文档说"准备发版"而实际"已部署"长达 24 天。

**修复**：本 release notes 状态行 + commit 行 + 时间线均已更新为实际部署状态。v0.10.18.md 同一错位已同步修正。

**教训**（写入 [AGENTS.md §1.2 文档一致性](../../AGENTS.md)）：
1. 发布流程最后一步应包含"部署完成 → 更新 release notes 状态 → close 文档 ticket"，而非"git push tag → 自认为结束"
2. 启动日志 version 字段必须用 ldflags 注入 `git describe --tags --always`（v0.9.2 硬编码误导 24 天，PR-B 待办已记录在 [ACTION-ITEMS-ECS-EXPIRY-2026-08.md §B2](../deployment/ACTION-ITEMS-ECS-EXPIRY-2026-08.md)）
3. CI/CD 应在 deploy job 末尾加 "update release notes status" 步骤（避免人为失误）

本次 3 个错位（v0.10.17 仅状态未错位 / v0.10.18 / v0.10.20）一并修正，作为 v0.10.21 的 release notes 准备前置工作。
```

---

## 🛡 部署后验证 checklist

| # | 项 | 期望 |
|---|----|------|
| 1 | 启动日志 | `INFO concurrency limiter initialized max_concurrent_trials=5` |
| 2 | `/metrics` 端点 | 4 个新 metric 出现（global_concurrency_* / session_rate_limit_rejected_total）|
| 3 | 5 个用户同时 StartTrial | 前 5 个 HTTP 200，第 6 个后台失败 + WS 收到 CONCURRENT_TRIAL_LIMIT |
| 4 | 同 session F5 狂点 10 次 action | 前 5 次 200，后 5 次 HTTP 429 + code=1427 |
| 5 | 跑 1 周观察 dashboard | 4 个 metric 实时反映限流触发率 |

---

## 📚 学习价值（面试用）

**简历一句话**：

> "为 DecisionCourt 设计 **4 层限流防御深度**（L3 Per-IP 令牌桶 + L2 Per-User 滑动窗口 + L1 Per-Session 令牌桶 + L0 全局信号量），实现 19 个测试覆盖 4 种限流算法的核心契约。L0 实装踩了双重 acquire bug，最终只在 service.withCancel 一处管理；L1 实装踩了 Release 阻塞 bug，改为 select default 非阻塞释放。"

**5 分钟讲法**：见 [interview/12 §11 一句话开场白 + §12.7 5 分钟讲法结构](../interview/12-rate-limit-defense-in-depth.md)。

---

## ⏸ 已知遗留 / 下一步

| 项 | 原因 | 计划 |
|----|------|------|
| **Config 化 L0 max** | 当前 `main.go` 硬编码 5 | DAU > 5000 时改为 `RATE_LIMIT_MAX_CONCURRENT_TRIALS` env |
| **Grafana dashboard** | 4 个 metric 已采集 | Phase C Prometheus 接入后做可视化 |
| **Redis backend** | L2 仍用 sync.Map | DAU > 5000 时切 Redis |
| **Per-Agent breaker** | prosecutor breaker 打开时 defender 仍能工作 | v0.11+ |
| **预存 agent prompt 测试** | 与本次无关 | 单独修复 |

---

## 🔗 关联文档

- [ADR 0027 · 4 层限流防御深度](../adr/0027-rate-limit-defense-in-depth.md) — 设计决策 + 实施后 3 处差异
- [interview/12 · 4 层限流防御深度](../interview/12-rate-limit-defense-in-depth.md) — 详细学习文档（算法 + 实战 + 5 个教训 + 10 个面试问答）
- [ADR 0014 · 用户级 Trial 限流](../adr/0014-user-rate-limit.md) — L2 Per-User 已有基础
- [ADR 0013 · LLM Gateway 工程化](../adr/0013-llm-gateway-engineering.md) — 限流的姊妹（熔断 / 缓存）
- [release-notes/v0.10.18.md](./v0.10.18.md) — 前一版本发布说明

---

## 🙏 致谢

感谢用户 2026-07-12 的"加面试能写的后端亮点"需求，让我从"先调研再实装"的工程纪律出发，发现了 2 个隐藏的集成 bug（双重 acquire / Release 阻塞）。这两个 bug 是单元测试捕获不到的，**只有集成测试 + 边界 case 测试才能暴露**——这是这次实装最宝贵的工程教训。